#include <iostream>
using namespace std;

int main() {
	/*
	int numbers[100], n;
	cin >> n;
	for (int i = 0; i <= n; i++) {
		cin >> numbers[i];
	}
	for (int i = 0; i <= n; i++) {
		for (int j = 0; j < n - i; j++) {
			if (numbers[j] < numbers[j + 1]) {
				swap(numbers[j], numbers[j + 1]);
			}
		}
	}
	for (int i = 0; i <= n; i++) {
		cout << numbers[i] << endl;
	}

	*/
	//system("pause");
	return 0;
}﻿
