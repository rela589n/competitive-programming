#include <iostream>
#include <vector>

using namespace std;

typedef long long Int;
void qsort(vector<Int> arr) {

}

int main() {

	int n;
	cin >> n;

	vector<int> arr(n);
	for (int i = 0; i < n; ++i) {
		cin >> arr[i];
	}


	system("pause");
	return 0;
}